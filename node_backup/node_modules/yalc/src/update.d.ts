import { PackageInstallation } from './installations';
export interface UpdatePackagesOptions {
    safe?: boolean;
    noInstallationsRemove?: boolean;
    workingDir: string;
}
export declare const updatePackages: (packages: string[], options: UpdatePackagesOptions) => PackageInstallation[];
